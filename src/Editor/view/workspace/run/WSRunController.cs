using System;
using Core.model;

namespace Editor.view.workspace.run
{
    public class WSRunController : WSController
    {
        public WSRunController(Model model)
        {
            Model = model;
        }
    }
}
